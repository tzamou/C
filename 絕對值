#include<stdio.h>
#include<stdlib.h>

int main(void)
{
	int num;
	while(true)
	{
	printf("輸入一個數: ");
	scanf("%d",&num);
	if(num>=0)
		printf("%d 的絕對值是 %d\n",num,num);
	if(num<0)
		printf("%d 的絕對值是 %d\n",num,num*(-1));
	}
	system("pause");
	return 0;
}
